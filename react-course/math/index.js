function add(x, y) {
  return x + y;
}

function substract(x, y) {
  return x - y;
}

function multyply(x, y) {
  return x * y;
}
function divide(x, y) {
  return x / y;
}

module.exports = {
  add,
  substract,
  multyply,
  divide,
};
